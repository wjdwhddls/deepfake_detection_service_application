# JVM 메모리 여유 (RN + AGP 8.8 권장)
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# 안드로이드X/제티파이어 (레거시 lib 호환)
android.useAndroidX=true
android.enableJetifier=true

# Gradle 기본 최적화
org.gradle.daemon=true
org.gradle.caching=true
# (선택) 병렬 빌드가 안전한 프로젝트면 켜도 됨
# org.gradle.parallel=true

# 경고를 다 보고 싶으면 (선택)
# org.gradle.warning.mode=all

# Kotlin 빌드 최적화
kotlin.code.style=official
kotlin.incremental=true

# (유지) RN 설정
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64
newArchEnabled=true
hermesEnabled=true

MYAPP_UPLOAD_STORE_FILE=my-release-key.keystore
MYAPP_UPLOAD_KEY_ALIAS=my-key-alias
MYAPP_UPLOAD_STORE_PASSWORD=kisook2557
MYAPP_UPLOAD_KEY_PASSWORD=kisook2557

# Workaround for Gradle 8.x and AGP 8.x compatibility issues with some RN libraries
# 아래 두 플래그는 AGP 8.x에서 더 이상 사용되지 않거나 표준 옵션이 아닐 수 있어 주석 처리
# 필요한 라이브러리에서 요구할 때만 일시적으로 활성화하세요.
# android.experimental.legacyTransform.forceNonIncremental=true
# android.jetifier.ignorelist=net.minidev:json-smart